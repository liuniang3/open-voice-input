# Open Voice Input 长音频记录与总结系统需求规格

> 文档用途：供 OpenCode / Grok 4.5 实现 Agent、审查 Agent 和 Codex 监督者共同读取。
>
> 本文是本次重大升级的统一需求来源。实现前必须完整阅读，不得只根据单次对话或局部任务推断整体目标。
>
> 更新日期：2026-07-19

## 1. 项目目标

将 Open Voice Input 从单一的 Windows 语音输入助手升级为一个本地优先的语音工作台，同时支持快捷输入、长时记录与总结、已有媒体文件处理。

项目需要保留当前语音输入功能，并新增以下三种相互隔离的工作模式：

1. **语音输入模式**
   - 保留全局快捷键、小型悬浮录音窗口、ASR、可选文本清理、剪贴板和光标输入。
   - 不得因为会议功能升级而增加该模式的启动延迟或破坏现有行为。

2. **长时记录与总结模式**
   - 同时记录电脑系统输出和麦克风。
   - 适用于腾讯会议、在线课程、访谈、讨论以及十几分钟到数小时的个人独白。
   - 结束后生成忠实原文、上下文校订版和结构化总结。

3. **媒体文件处理模式**
   - 导入已有音频或视频文件。
   - 复用相同的音频规范化、ASR、说话人识别、校订和总结管线。

这不是在当前短录音界面中增加一个大按钮，而是为项目引入第二套面向持久会话的可靠数据流。

## 2. 当前能力基线

升级过程中必须保留以下已有能力：

- Electron 无边框 Windows 应用和任务栏托盘。
- 全局快捷键录音及可配置麦克风。
- MiMo、Qwen3-ASR、Fun-ASR 供应商抽象。
- Fast 与 Stable 两种转写模式。
- 实时转写预览。
- ASR 与文本清理模型独立 URL、Key 和模型档案。
- 16 kHz、单声道、16-bit PCM 音频规范化。
- 根据供应商限制进行长音频分段和顺序拼接。
- 剪贴板复制、焦点应用粘贴和上一次转写重试。

不得重置、覆盖或回退工作区中不属于当前任务的已有改动。

## 3. 核心功能需求

### 3.1 长时会话

- 支持新建、暂停、恢复、结束和取消记录会话。
- 显示录制时长、音源状态、音量、磁盘空间、网络状态、转写队列和错误状态。
- 录制过程中允许添加书签和临时文字笔记。
- 录音必须先可靠写入本地，网络请求和模型调用不能成为录音链路的前置条件。
- 网络断开、API 超时、限流或模型失败时继续录音，网络恢复后继续处理。
- 程序异常退出或电脑重启后能够发现并恢复未完成会话。

### 3.2 双音轨采集

- 系统输出和麦克风必须作为两个独立音轨保存，并使用单调时钟保持时间同步。
- 麦克风必须通过 Windows WASAPI 共享模式采集，不能独占设备，不能导致腾讯会议无法使用麦克风。
- 第一阶段必须支持选择系统输出设备和麦克风设备。
- 后续支持按应用或进程捕获腾讯会议等指定程序声音；实现前需验证目标 Windows 版本对 Process Loopback Capture 的支持。
- 需要处理系统音频和麦克风重复收音、回声、监听返送以及设备切换造成的时间漂移。
- 录音过程中禁止应用自身播放提示音进入待转写音轨，或需要提供明确的排除策略。

### 3.3 持久化与恢复

- 建议每 1 至 5 分钟生成一个独立音频段，不允许把数小时音频只保存在内存中。
- 每段记录会话 ID、音轨来源、单调时间范围、采样参数、文件大小、校验值和处理状态。
- 写入过程需要使用临时状态和原子完成标记，避免程序崩溃后把半写入文件误认为完成。
- 正常录制不得丢失音频；强制结束时最多允许损失最后一个很短的未落盘缓冲区。
- 会话恢复后不得重复上传、重复拼接或重复生成同一个分段结果。

推荐的会话目录结构：

```text
sessions/<session-id>/
  session.json
  jobs.json
  audio/microphone/000001.<format>
  audio/system/000001.<format>
  transcript/raw.jsonl
  transcript/corrected.jsonl
  transcript/corrected.md
  summary/summary.json
  summary/summary.md
  notes.jsonl
```

具体格式由架构阶段确定，但必须满足可恢复、可校验和可迁移要求。

### 3.4 ASR 与模型限制

- 根据供应商、模型、接口模式和输入格式维护独立的能力档案。
- 能力档案至少包含最大文件大小、最大时长、支持格式、实时/批处理方式、时间戳、说话人识别、热词、上下文增强和限流信息。
- 不允许使用一个硬编码阈值覆盖所有 ASR 模型。
- 分段优先选择静音或语义边界；达到硬上限前必须强制结束当前段。
- 分段转写结果按时间顺序缓存和拼接，不得丢弃超限部分。
- 每段失败需要独立重试，已经成功的段不得重复计费，除非用户主动执行完整重试。

当前已知限制：

- MiMo `mimo-v2.5-asr`：WAV/MP3，Base64 编码字符串最大 10 MB。
- Qwen3-ASR-Flash：单个音频最大 10 MB、最长 5 分钟。
- Fun-ASR / Qwen3-ASR-Flash-Filetrans 异步文件接口：单文件最大 2 GB、最长 12 小时。
- Qwen/Fun 实时接口使用 WebSocket 音频流，不应在结束时再次上传完整高质量 WAV。

来源：

- [MiMo-V2.5-ASR 语音识别](https://mimo.mi.com/docs/zh-CN/quick-start/usage-guide/audio/Speech-Recognition)
- [阿里云非实时语音识别](https://help.aliyun.com/zh/model-studio/non-realtime-speech-recognition-user-guide)
- [阿里云实时语音识别](https://help.aliyun.com/zh/model-studio/real-time-speech-recognition-user-guide)

### 3.5 说话人识别

- 麦克风音轨可直接标记为“我”或本机用户，避免把本机讲话交给模型重复区分。
- 系统输出音轨中的远端参与者使用 Fun-ASR 等具备说话人分离能力的批处理模型处理。
- 不得假设所有实时 ASR 模型都支持说话人分离；实时预览和会后权威转写可以使用不同模型。
- 说话人结果需要包含稳定 ID、时间范围和置信信息。
- 用户可以将“说话人 1”重命名为真实姓名，并同步更新校订版和总结。
- 两个音轨中出现相同内容时需要做时间对齐和重复检测，不能在最终原文中重复一遍。

### 3.6 转写版本

必须区分以下内容：

1. **原始转写**：ASR 的忠实结果，只允许追加和标注，不得被 LLM 覆盖。
2. **校订版**：结合上下文、词表和全局语境修正明显识别错误、标点和专有名词。
3. **结构化总结**：对内容进行提取、组织和归纳，不替代原文。

校订必须满足：

- 保存原始片段 ID、时间戳和修改差异。
- 不确定的人名、术语和数字不得静默猜测，应保留原文或标记待确认。
- 模型不得新增录音中不存在的事实。
- 用户可以查看原文与校订版差异，并回退单个修改。

### 3.7 结构化总结

会议场景至少输出：

- 执行摘要。
- 会议主题和层级化大纲。
- 按时间排列的讨论进程。
- 每位说话人的主要观点。
- 已确认的决定。
- 行动项、负责人和截止时间。
- 未解决问题、分歧和风险。
- 关键原话及对应时间戳。

个人长语音场景至少输出：

- 核心想法。
- 层级化论述框架。
- 支撑理由、案例和假设。
- 尚未展开的问题。
- 可执行的下一步。
- 完整原文和校订版入口。

系统应根据内容自动选择会议模板或个人思考模板，同时允许用户手动切换。

## 4. 长上下文处理要求

不得只把上一段的普通摘要拼接到下一段，也不得依赖单次请求强行容纳全部原文。

推荐流程：

```text
原始转写分段
-> 每段提取事实、观点、决定、行动项、实体、说话人与证据时间戳
-> 维护可验证的滚动状态
-> 汇总全部结构化中间结果
-> 执行最终全局整理
-> 返回原始片段抽样或逐项核验
-> 生成最终结果
```

具体要求：

- 按语义、说话人和时间边界切分文本，而不是机械按字符数切分。
- 每个中间结果保存来源片段 ID 和时间范围。
- 滚动状态包含已确认事实、决定、行动项、实体表、说话人观点和未解决问题。
- 新片段处理时可以使用压缩状态作为背景，但不得因此丢弃旧片段的证据索引。
- 最终归并使用全部中间结果和必要的关键原文，不仅使用最后一次滚动摘要。
- 最终决定、行动项和关键观点必须能追溯到原文。
- 对结构化结果再执行一次一致性检查，识别遗漏、矛盾和无证据内容。

## 5. 模型配置与选择

新增独立于现有“文本清理模型”的“长内容整理模型”配置。

模型能力档案至少记录：

- 供应商、模型 ID、Base URL 和独立 Key 引用。
- 上下文窗口和最大输出。
- 是否支持结构化输出和推理模式。
- 实际可用的推理等级。
- 请求超时、价格、限流和并发。
- 适合的任务类型：片段提取、校订、最终归并或核验。

模型选择规则：

- 实际输入预算最多使用标称上下文的约 60% 至 70%，为输出和误差保留余量。
- 内容能够安全放入上下文时，可以执行全量整理，但仍需证据核验。
- 超出上下文时必须使用分层 Map/Reduce 与滚动状态。
- 最终全局整理优先使用高能力模型，例如测试渠道可用的 GPT-5.5 High、Grok 4.5 或 MiMo V2.5 Pro。
- 不得仅根据模型宣传页假设能力，必须以当前渠道的实际连通性、有效上下文和结构化输出测试为准。
- 片段提取和最终归并可以使用不同模型，但最终质量优先于最低成本。

## 6. 媒体文件导入

- 支持常见音频和视频文件。
- 视频导入后自动提取音频，不需要用户手动转换。
- 推荐评估打包 FFmpeg 或使用 Windows Media Foundation，并明确许可证和安装包体积影响。
- 大文件导入使用流式读取和分段处理，不得一次性全部载入内存。
- 导入任务支持暂停、恢复、失败重试和程序重启后继续。
- 已有多声道音频需要保留声道信息，并允许选择混音或独立转写。

## 7. 前端与交互要求

前端继续使用项目自己的无边框界面，不调用 Windows 默认设置窗口，不出现系统菜单栏与应用内标题栏叠加。

主要页面：

1. **语音输入页**：保持当前轻量悬浮录音体验。
2. **记录控制台**：音源选择、双轨音量、时长、暂停、恢复、结束、磁盘、网络和丢帧状态。
3. **实时转写页**：按时间和说话人显示内容，支持书签和临时笔记。
4. **会话结果页**：原始转写、校订文本、结构化总结三个主要视图。
5. **媒体导入页**：文件选择、解析信息、处理进度、队列和错误恢复。
6. **历史记录页**：搜索、恢复、重命名、删除、导出和继续处理。
7. **设置页**：录音设备、模型档案、凭证、存储位置、总结模板和数据保留策略。

结果页至少支持：

- 音频播放时间轴与文本联动。
- 点击时间戳跳转到对应音频。
- 说话人颜色、筛选和名称修改。
- 原文与校订版差异查看。
- 全文搜索。
- 左右分栏或清晰标签页切换。
- Markdown、JSON、TXT 和 SRT 导出。
- 长文本虚拟滚动或等效性能优化。

视觉要求：

- 延续当前克制、清晰、工作导向的深色界面。
- 不能把所有页面堆成装饰性卡片，不能出现卡片嵌套卡片。
- 工具按钮优先使用项目现有图标库或 Lucide 图标，并提供工具提示。
- 录音、暂停、恢复、处理中、失败、离线和恢复状态必须清楚但不过度打扰。
- 说话人颜色需要兼顾可读性、色盲可辨识性和深色背景对比度。
- 不得出现文本溢出、按钮跳动、控件重叠或窗口尺寸变化导致的布局错乱。
- 必须检查常见桌面分辨率、Windows 缩放比例和长文本场景。

## 8. 安全、隐私与合规

- API Key 不得进入源码、Git、日志、截图、测试样本或 Agent 提示词。
- 后续应将 Key 从明文设置文件迁移到 Windows Credential Manager 或 DPAPI。
- 音频、转写和总结默认保存在本机，不自动上传与当前任务无关的内容。
- 录制必须有明确可见状态，并提供暂停、停止和取消。
- 需要在产品中提示用户遵守会议录音授权和所在地法律。
- 日志只记录任务 ID、状态、耗时、大小和错误类型，不记录完整私人转写。
- 用户可以配置数据保留期限，并能够安全删除整个会话及派生文件。

## 9. 非功能性要求

- 本地录音优先级高于实时转写、总结和界面动画。
- ASR 与总结任务使用持久任务队列，支持幂等重试和指数退避。
- 长时间录制需要监控磁盘空间、写入延迟、音频丢帧、设备断开和时间漂移。
- 录音和转写不能阻塞 Electron 渲染线程。
- 大文件和长文本不得造成界面冻结或内存无限增长。
- 不同模式、会话和模型配置之间必须隔离，不能带入上一次转写上下文。
- 开发版、安装版和便携版必须使用一致的数据目录和恢复规则。

## 10. Agent 实施方法

本次重大升级采用监督式实现，而不是由 Codex 直接编写功能代码。

### 10.1 角色分工

- **Codex**：架构负责人、任务拆分者、监督者、测试执行者和最终审查者。
- **OpenCode Grok 4.5 High**：实际实现代码、测试和文档。
- **独立 Grok 审查 Session**：进行只读或限制写入范围的对抗性审查。

除非用户另行授权，Codex 不直接修改实现代码。Codex 发现问题后，应向负责该范围的 Grok Session 提供明确缺陷、文件位置、复现步骤和验收条件，并要求其修复。

### 10.2 模型供应商顺序

1. 优先使用 NowCoding 供应商的 Grok 4.5，高思考状态。
2. 只有在 NowCoding 明确出现额度耗尽、限流或持续不可用时，才切换到 Aixoras Grok 4.5。
3. 切换供应商时需要给新 Session 提供经过脱敏的任务摘要、当前 diff、测试结果和未解决问题。
4. 不得把任何供应商 Key 写入 Session 提示词、命令输出或仓库文件。

### 10.3 Session 规则

- 每个 Session 必须有明确目标、文件边界、输入输出协议和验收标准。
- 实现前先输出计划，由 Codex 审查后再进入编码。
- 不允许多个 Session 同时修改相同文件或共享协议。
- 并行任务必须使用独立 Git worktree 或严格不重叠的文件范围。
- 主进程入口、会话数据协议、任务状态机和公共类型必须串行修改。
- 不允许使用破坏性 Git 命令，不得回退用户已有改动。
- 涉及文件删除时应移动到回收站或安全废弃目录，不直接永久删除。

每个实现 Session 完成时必须报告：

- 修改的文件。
- 实现的行为。
- 运行的测试及结果。
- 未完成事项。
- 已知风险。
- 是否修改配置格式或需要迁移。

## 11. 建议实施阶段

### 阶段 0：架构验证

- 验证 Windows WASAPI 系统输出捕获、麦克风共享和双轨同步。
- 验证腾讯会议仍能正常使用麦克风。
- 决定原生辅助进程采用 Rust、C++、现有库或其他可靠方案。
- 决定音频分段格式、会话清单和崩溃恢复协议。
- 由第二个 Grok Session 对架构方案做对抗性审查。

没有通过阶段 0，不得开始大规模前端或总结引擎开发。

### 阶段 1：录音与会话存储

- 实现双音轨采集、分段落盘、会话清单、暂停恢复和崩溃恢复。
- 暂不依赖云端 ASR 完成录音验收。

### 阶段 1A 契约补充（2026-07-20，实现已落地基础库）

- 会后权威 ASR 输入为**按轨 mono PCM16 WAV 归档**（设备采样率；非 L1 HQ），并附 sidecar 映射 seq/QPC/pause gaps。
- 暂停空洞不得用臆造语音填补；Stage 1A 默认不在 WAV 中插入静音，仅在 sidecar `gaps[]` 显式记录。
- 云端 Fun-ASR 异步 `file_urls` 需要**公网 HTTPS URL**。默认 `MeetingAudioPublisher` 为本地 export-only、永不上传；未配置可产生公网 URL 的 publisher 时必须给出可操作错误。对象存储接入仍为未决产品项。
- 麦克风轨说话人固定为本地 `self`；系统轨使用 Fun-ASR diarization speaker id。两轨转写时间线可重叠保留，本阶段不做回声消除或转写前混音。

### 阶段 2：ASR 与说话人识别

- 实现持久任务队列、供应商策略、分段转写、时间戳、说话人识别和失败恢复。
- 建立原始转写数据格式。

#### 阶段 2A 默认策略补充（2026-07-20，无 Bucket）

- **默认**会议转写路径不依赖用户对象存储（no-Bucket-first）：使用 Qwen3-ASR-Flash 本地 Base64/data URI 分段（目标约 180s @16 kHz mono PCM16；硬预检 duration≤300s、Base64/data URI≤约 10 MiB）。
- Qwen 路径 **不提供**远端说话人分离；麦克风轨标记为本地 `self`，系统轨标记为单一 `remote_unknown`。不得在无 diarization 时臆造多个远端 speaker id。
- 时间戳默认仅为 **段级**（`timestampPrecision: "segment"`），对应分段边界经 Stage 1A sidecar 映射到会话时间。
- 原始转写与任务状态落盘在会话目录；日志不得写入完整转写或 API Key。
- 北京工作区 OpenAI 兼容 Base URL 模板（无密钥）：`https://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`。对该 MaaS 端点应使用 `/compatible-mode/v1`，**不要**使用 `/api/v1`。
- **可选**后续：Fun-ASR + 可产生公网 HTTPS URL 的 publisher（及 OSS 等）**仅**用于远端 diarization；非 2A 默认，且不得静默上传。

#### 阶段 2B IPC 补充（2026-07-20）

- 会后处理必须由显式 IPC（`meeting:process:start`）触发；采集 stop **不得**自动 export/ASR。
- 会议 Qwen 凭证仅运行时解析（`OVI_MEETING_*` / `QWEN_ASR_*` / `DASHSCOPE_API_KEY`），不与短语音 provider profile 静默绑定，不写入 session/job。
- Base URL 主名为 `OVI_MEETING_DASHSCOPE_BASE_URL`；`/api/v1` 明确拒绝。status/list 不含转写正文与绝对路径。

#### 阶段 3A 分析补充（2026-07-20）

- 权威输入 `transcription/qwen-no-bucket/raw-transcript.json` **不可变**；输出为独立的校订转写与结构化总结。
- 长内容整理模型凭证与短语音 cleaner / 会议 Qwen ASR **隔离**（`OVI_MEETING_ANALYSIS_*`），不落盘。
- 统一分批 correct→extract→merge→本地证据校验；结论必须带 `sourceItemIds`。无 UI。

#### 阶段 4A 会议工作台 UI 补充（2026-07-20）

- 独立窗口模式 `meeting`，托盘/顶栏进入；采集 stop 与「生成原文 / 校订并总结」分离。
- 会议设备与凭证字段与短语音设置隔离；结果 DOM 使用 textContent，不注入 HTML。
- 未含：媒体导入、音频播放联动、说话人重命名、会话删除。

#### 阶段 4B-core 导入/导出/回放/显示名（2026-07-20）

- WAV 导入 → 标准 archive sidecar；停止态；用户显式生成原文，复用 2B/3A 管线。
- 导出会话 Markdown/JSON/TXT/SRT；speaker-map 仅装饰；受控 `mimo-meeting` 回放 token；长转写虚拟列表。

#### 阶段 4B-video 媒体导入（2026-07-20）

- 打包 FFmpeg（prepare/check + extraResources）；无 shell 抽取首音轨为 16k mono PCM16 archive。
- 支持常见音视频扩展名（上限 8GiB）；源文件只复制；与 WAV 共用 commit/quarantine。
- UI「导入媒体…」+ 导入角色；playback track=auto；hasArchive 门控生成原文。
- 未含：双轨 scrub、会话删除、OSS；不宣称全 codec/超长视频验收。

#### 阶段 4C 增强转写 UI（2026-07-20）

- 工作台基础/增强模式按钮 + 32/48/64 kbps；start/retry 传 mode+bitrate；处理中禁切换。
- 设置会议页：默认模式/质量、Fun-ASR、OSS 全字段与测试连接；敏感字段显示/复制。
- 说话人 select + 名称 + 保存；phase/remoteCleanup 状态提示；浅色毛玻璃工作台视觉。
- 默认仍为 basic 零上传；增强才走 OSS + Fun 系统轨分离。

### 阶段 3：校订与总结引擎

- 实现模型能力档案、Token 预算、分层整理、滚动状态、最终归并和证据核验。
- 建立校订版和结构化总结格式。

### 阶段 4：前端工作台

- 实现记录控制台、实时转写、结果页、文件导入、历史记录和设置。
- 完成桌面视觉、响应式布局、缩放比例和长文本检查。

### 阶段 5：强化与发布

- 完成长时间、断网、崩溃、设备切换、模型限流和超大文件测试。
- 完成安装包、便携版、数据迁移、密钥扫描和发布文档。

## 12. 验收标准

- 连续录制两小时腾讯会议，腾讯会议始终能够正常使用麦克风。
- 系统音轨和麦克风音轨无明显缺段，并能按时间对齐。
- 断网、API 失败和供应商限流不会中断本地录音。
- 强制结束应用后重启，可以恢复会话，最多损失最后一个短缓冲区。
- 所有供应商请求均不超过对应模型的大小、时长、格式和上下文限制。
- 多人会议能够生成说话人标记、原始转写、校订版和结构化总结。
- 十几分钟个人独白能够完整保留原文并生成可靠的论述框架。
- 本地音频和视频导入能够复用相同处理管线。
- 决定、行动项和关键观点均可跳转到原文时间戳。
- 校订和总结不得覆盖原始转写，不得无依据新增事实。
- 大型会话不会造成界面冻结、无限内存增长或录音中断。
- 常见桌面分辨率和 Windows 缩放比例下没有文本溢出、按钮跳动或控件重叠。
- 安装包不包含 API Key、测试音频、本机会话或私人转写。
- 当前语音输入模式全部回归测试通过。

## 13. 第一项执行任务

正式实施时，第一项任务不是直接编码完整系统，而是：

1. 启动一个 NowCoding Grok 4.5 High 架构 Session。
2. 要求其检查当前代码并输出 Windows 双音轨采集、持久会话存储、任务队列和恢复机制的详细技术方案。
3. 启动第二个 Grok 4.5 High Session 对该方案进行对抗性审查。
4. 由 Codex 汇总冲突、要求修订并确认阶段 0 的可验证原型范围。
5. 只有方案和验证标准通过后，才委派录音层实现。

## 14. 暂不包含的范围

除非用户后续明确要求，第一版不包含：

- 自动加入在线会议的机器人。
- 多用户云端协作编辑。
- 云同步和跨设备账号系统。
- 实时翻译。
- 自动读取日历并静默开始录音。
- 未经用户明确启动的后台录音。

